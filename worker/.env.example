# ── WarmCup Worker — Environment Variables ────────────────────
# Secrets are set via Wrangler, not .env files.
# Run:  wrangler secret put SECRET_NAME
#
# Currently no secrets required for basic deployment.
# Add here if you wire up Stripe webhook verification:
# STRIPE_WEBHOOK_SECRET=whsec_...
